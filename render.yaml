services:
  - type: web
    name: ascode-scraper
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 10000 --timeout-keep-alive 600"
    envVars:
      - key: SECRET_KEY
        value: your-secret-key
